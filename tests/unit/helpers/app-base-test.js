
import { appBase } from 'church/helpers/app-base';
import { module, test } from 'qunit';

module('Unit | Helper | app base');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = appBase([42]);
  assert.ok(result);
});

