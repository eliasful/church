
import { concatenate } from 'church/helpers/concatenate';
import { module, test } from 'qunit';

module('Unit | Helper | concatenate');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = concatenate([42]);
  assert.ok(result);
});

