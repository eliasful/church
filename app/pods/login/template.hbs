<header></header>
<div class="container login-container">
  <div class="row justify-content-md-center">
    <div class="col-lg-4">
      <div class="card">
        <div class="card-body">
          <form {{action 'authenticate' on='submit'}}>
            <h2 class="text-center">
              <i class="fa fa-sign-in-alt"></i> Login
            </h2>
            <div class="form-group">
              <label>E-mail</label>
              {{input type="email" class="form-control" value=identification required=true autofocus=true placeholder="exemplo@exemplo.com"}}
            </div>
            <div class="form-group">
              <label>Senha</label>
              {{input type="password" class="form-control" value=password required=true placeholder="******"}}
            </div>
            <button type="submit" class="btn btn-primary btn-block">Continuar</button>
          </form>
        </div>
      </div>
    </div>
  </div>
  <div class="row justify-content-md-center">
    <div class="col-lg-4">
      <hr>
      <div class="text-center text-muted ">
        Não possuí cadastro? {{#link-to 'church.new'}}Cadastrar nova igreja {{/link-to}}
      </div>
    </div>
  </div>
</div>
